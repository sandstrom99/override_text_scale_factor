name: example
description: An example project for override_text_scale_factor package.

version: 2.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.11.0
  flutter_test:
    sdk: flutter
  override_text_scale_factor:
    path: ../

flutter:
  uses-material-design: true
